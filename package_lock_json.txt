1) It started after version 5
2) We don't need node_modules we need only package.json
3) Need of package-lock : It will lock all packages and dependenies to reduce compatancy issues. It helps in consistent node_modules even if we use another system
